doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 공지사항
        ul
            each topic in topics
                li
                    a(href='/notice?id='+topic.Nid)= topic.title
        
        //article 
            if detail
                h2= detail.title
                = detail.description
                div= 'by ' + detail.author
            else
                h2 Welcome
                no content//
        div
            a(href='/notice/add') 글쓰기